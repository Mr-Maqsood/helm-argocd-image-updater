apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-stack.fullname" . }}
  labels:
    {{- include "flask-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    {{- include "flask-stack.selectorLabels" . | nindent 4 }}
